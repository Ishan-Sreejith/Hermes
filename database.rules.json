{
  "rules": {
    ".read": true,
    ".write": false,
    "rooms": {
      ".read": true,
      ".write": true,
      "$room": {
        ".validate": "newData.val() === null || (newData.hasChildren(['created_at']) && newData.child('created_at').isNumber() && newData.child('created_at').val() > 0 && (!newData.child('created_by').exists() || (newData.child('created_by').isString() && newData.child('created_by').val().length <= 128)) && (!newData.child('password').exists() || (newData.child('password').isString() && newData.child('password').val().length <= 128)))"
      }
    },
    "presence": {
      ".read": true,
      "$uid": {
        ".write": "true",
        ".validate": "newData.val() === null || (newData.hasChildren(['ts']) && newData.child('ts').isNumber() && (!newData.child('status').exists() || newData.child('status').isString()) && (!newData.child('ip').exists() || (newData.child('ip').isString() && newData.child('ip').val().length <= 64)) && (!newData.child('port').exists() || newData.child('port').isNumber()))"
      }
    },
    "users": {
      "$username": {
        ".read": true,
        ".write": "true",
        ".validate": "newData.val() === null || (newData.hasChildren(['password','peer_id']) && newData.child('password').isString() && newData.child('password').val().length === 64 && newData.child('peer_id').isString() && newData.child('peer_id').val().length <= 128)"
      }
    },
    "messages": {
      "$chatKey": {
        ".read": true,
        ".indexOn": ["ts"],
        "$msgId": {
          ".write": "true",
          ".validate": "newData.val() === null || (newData.hasChildren(['id','body','fromId','fromName','ts']) && newData.child('id').isString() && newData.child('id').val().length <= 128 && newData.child('body').isString() && newData.child('body').val().length > 0 && newData.child('body').val().length <= 1000 && newData.child('fromId').isString() && newData.child('fromId').val().length <= 128 && newData.child('fromName').isString() && newData.child('fromName').val().length <= 40 && newData.child('ts').isNumber() && newData.child('ts').val() > 0)"
        }
      }
    },
    "signaling": {
      ".read": true,
      ".write": true,
      "$node": {
        ".validate": "newData.val() === null || newData.isString() || newData.isNumber() || newData.isBoolean() || newData.hasChildren()"
      }
    }
  }
}
